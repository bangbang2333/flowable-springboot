server:
  port: 8081
  servlet:
    context-path: /

spring:
  profiles:
    active: local
  application:
    name: demo-server
    default-token: NzE5NzhFOEFDNTdFQzIxRUVFMjU2RUFBQzhBMjAxNDYxNkVCRjM2NjMwMkMwNDA2NTkwQThERUIzM0U5MTNDNjlEMEY4NUFBMjI4RDhDM0FEQjk2RjQ3QzdFQzgwM0Iy
  datasource:
    url: jdbc:p6spy:mysql://rm-bp14nmd1q0bg5iw0xvo.mysql.rds.aliyuncs.com:3306/oa?serverTimezone=CTT&characterEncoding=utf8&useUnicode=true&autoReconnect=true&allowMultiQueries=true&useSSL=false&zeroDateTimeBehavior=CONVERT_TO_NULL&useAffectedRows=true
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
    username: root
    password: 5%-yLhEbty
    type: com.alibaba.druid.pool.DruidDataSource
  aop:
    proxy-target-class: true
  # Redis数据库索引（默认为0）  
  redis:
    database: 1
    host: 47.96.68.24
    port: 6379
    password: 0ks9jz7x
    timeout: 5000
    # 连接池最大连接数（使用负值表示没有限制）
    lettuce:
      pool:
        max-active: 10  # 最大活跃链接数 默认8
        max-idle: 10  # 最大空闲连接数 默认8
        min-idle: 0 # 最小空闲连接数 默认0
        max-wait: 1000  # 连接池最大阻塞等待时间（使用负值表示没有限制）
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    serialization:
      write-dates-as-timestamps: false

mybatis-plus:
  configuration:
    cache-enabled: true
    call-setters-on-nulls: true
  global-config:
    db-config:
      logic-delete-value: 'null' # 逻辑已删除值(默认为 1)，null字符串mybatis-plus会转换为null处理
      logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)
  mapper-locations: classpath:mapper/*.xml